#include "tester.h"
#include <QDebug>
#include <QTimer>

using namespace QTest;

tester::tester()
{
   // connect(this, SIGNAL(testSignal()), this, SLOT(test()));
   // testSignal();

}

void tester::test(){

}
